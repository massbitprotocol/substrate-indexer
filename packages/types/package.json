{
  "name": "@massbit/types",
  "version": "0.1.1",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc -b"
  },
  "main": "dist/index.js",
  "files": [
    "src/global.d.ts",
    "/dist"
  ],
  "peerDependencies": {
    "@polkadot/api": "^6"
  },
  "devDependencies": {
    "@polkadot/api": "^6",
    "@types/app-module-path": "^2.2.0"
  }
}
